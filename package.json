{
  "name": "webcypressparabank",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:run:chrome:allure": "npx cypress run --browser chrome  --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "test:allure": "npm run cy:run:chrome:allure && npm run report:allure"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@faker-js/faker": "^9.6.0",
    "allure-commandline": "^2.33.0",
    "cypress": "^14.2.0",
    "mocha-allure-reporter": "^1.4.0"
  }
}
